apiVersion: v1
kind: Service
metadata:
  name: eval-poetry-min
  labels:
    app: eval-poetry-min
spec:
  type: NodePort
  selector:
    app: eval-poetry-min
  ports:
  - port: 80
    targetPort: 8000
